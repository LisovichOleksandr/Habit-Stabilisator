# Spring Boot + Docker + PostgreSQL

Этот проект представляет собой приложение для контроля и отслеживания привычек.  
С его помощью пользователь может создавать привычки, отмечать ежедневное выполнение,  
анализировать прогресс и формировать полезные дисциплины.  
Приложение работает на Spring Boot и использует PostgreSQL для хранения данных,  
а запуск осуществляется через `docker-compose`.

## 🚀 Стек технологий
- Java 17
- Spring Boot
- PostgreSQL 16
- Docker
- Docker Compose

---

## 📦 Как собрать и запустить проект

### 1. Собрать приложение

Перед запуском контейнеров необходимо собрать JAR-файл:

mvn clean package -DskipTests
После выполнения JAR-файл появится в директории target/.

###  2. Запустить проект через Docker
docker-compose up --build
Эта команда:
соберёт Docker-образ приложения
запустит контейнер с PostgreSQL
запустит Spring Boot приложение

После запуска приложение будет доступно по адресу: http://localhost:8080
База данных будет доступна на: localhost:5432

🔄 Как обновить приложение
mvn clean package -DskipTests
docker-compose down
docker-compose up --build -d
